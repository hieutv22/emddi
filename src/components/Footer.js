import React, {Component} from 'react'

export default class Footer extends Component {
    render(){
        return(
            <footer>
                <div className="dropdown">
                <button className="dropbtn">Đặt xe</button>
                </div>
            </footer>
        );
    }
}